{
  "name": "wojakmeter-bot-v2",
  "version": "0.1.0",
  "private": true,
  "description": "WojakMeter bot v2 — phase 1: the research lab (15-minute snapshots, history rebuild, pre-registered hypotheses) and its signed desk API. No Telegram, no trading, no exchange keys.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "express": "^4.22.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
